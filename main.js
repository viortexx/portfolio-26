// Main Javascript file for Portfolio
console.log("Vitoria Coelho Portfolio loaded.");

// You can add global interactivity here if needed later.
document.addEventListener('DOMContentLoaded', () => {
  // Ensure the custom cursor works well, maybe hide default cursor fully if needed
  // (Currently handled via CSS cursor property)
});
